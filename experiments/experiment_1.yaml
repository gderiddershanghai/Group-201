model:
  type: "LSTM"
  input_size: 300
  hidden_size: 128
  num_layers: 2
  dropout: 0.5
  bidirectional: true

training:
  batch_size: 64
  epochs: 25
  learning_rate: 0.001
  optimizer: "adam"
  
data:
  train_data: "/data/processed/train.csv"
  val_data: "/data/processed/val.csv"
